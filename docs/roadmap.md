# ロードマップ

現在はアルファ版（試作品）段階です。しばらくは互換性を気にせず破壊的な変更が続くと思います。

開発は以下の3フェーズで進めます。

## Phase 1: FoundryVTT v14対応

- [x] v14専用への移行（v13サポート打ち切り）— 完了（2026-07-19）

## Phase 2: リファクタリング

「動けばいい」で書いてきたコードを、適切な機能を適切な層に分離して責務をはっきりさせる形に書き換えます。設計方針と既知の課題は [アーキテクチャ](/architecture) を参照。

- [x] スキーマ定義の初期化順序依存（`CONFIG.EMOKLORE` / `game.i18n`）の解消
- [x] `EmokloreActor` から判定計算・ダイアログ・チャット生成を分離
- [x] シート層に混ざったルール計算を data 層へ移動
- [x] `as any` の削減（`noExplicitAny` を lint で `error` に）
- [x] 純粋関数（判定計算など）への vitest テスト導入
- [x] CSS・テンプレートのリファクタリング（部品と配置の2層化）
- [x] 見た目とロジックの分離。チャットカードの表示を `chat/` 層へ、シートのコンテキスト整形を `applications/context/` へ、技能の見せ方を1つの関数へ。武器カードのオーケストレータ化も解消

## Phase 3: 新機能

未着手分は要件をIssueに分割してあり、**詳細は各Issueが正**。リリースまでの全体はマイルストーン[正式版（v1.0）](https://github.com/ryotai-trpg/emoklore/milestone/2)で追う（#59 も含む）。

- キャラクターシートの機能追加・UI/UX向上
    - [x] UI設計の規約を明文化（`docs/ui-design.md`）
    - [x] 色設計の是正（全色をテキスト4.5:1／UI要素3:1に）
    - [x] ホバーとフォーカスの導入
    - [x] 英語表示の崩れ修正
    - [x] 編集の入力をスライダーから段のクリックへ
    - [x] 立ち絵と能力値をタブ共通の折りたためるサイドバーへ
    - [x] アイテムタブ追加
    - [x] カスタム技能（Item `skill`）
    - [x] 共鳴感情のApplication化（Issue #74）。技能まわりのselectには広げない（択が5〜13で素のselectで足りる）
    - 一般技能と基本技能の視覚言語を揃える。`＊` / `★` の印の扱いも一緒に決める（Issue #82）
    - ヘッダの再設計（Issue #83）
    - UIの一貫性のうち、値の判断が要る分（Issue #59）
- 判定まわりの拡充
    - [x] 共鳴判定のGM要求と感情マッチングの自動化。憑依判定と共鳴感情の追加取得（「共振」）の器を含む（Issue #75）
    - GMからの技能判定指示と判定オプション（Issue #85）
    - [x] HP/MP境界の状態処理の案内
- 武器・防具の実装
    - [x] 武器アイテムと武器シート
    - [x] 武器カード（1枚のカードに攻撃判定とダメージを積む）
    - [x] ダメージ適用（権限の無いアクターはGMのクライアントに委譲）
    - [x] ダメージ適用の結果を伏せられるようにする。設定で「全員／DLだけ／チャット欄の選択に従う」を選ぶ。本体の `messageMode` に写しているので、判定メッセージ側（#16）も同じ口に乗せられる
    - [x] 〈ストレングス〉による近接武器攻撃力の加算
    - [x] 回避・防御によるダメージ軽減
    - [x] 装備と防具。書籍版ルールブックに規定がある
- [x] NPC・怪異のシートとデータモデル（Issue #81）。人間NPCは共鳴者と同じ判定を流用する軽量版、怪異は直接判定の攻撃・共鳴プリセット・装甲・固定初速を持つ別形状。共鳴表（#79）との統合は参照を持つところまでで、引く処理は #79 に残す
- ハウリング（極限共鳴）の実装（Issue #79）。**RollTable + Item に一本化し、Cards は採らない** — 反応を Item サブタイプ `howling` にして効果を ActiveEffect に載せ、共鳴表（RollTable）がそれを引く。ハウリングカードと共鳴表はルール上も同じ6件の別表現なので区別しない
    - [x] `howling` アイテムの器（分類・効果・回復条件）
    - [x] 引く導線（共鳴結果カード → 共鳴表 → 反応カード → 適用）と効果タブのハウリング区分
    - [x] サンプルの同梱（汎用共鳴表「因縁タイプ」）と packs のビルド経路
    - **感覚のタグ（「視覚を利用する技能」）は見送る。** 公式75種を確認したところ、五感を条件にするのは 0020 幻視「視覚を使う技能での判定は成功数-1」の1件だけだった。48技能へのタグ付けと「〈運転〉は視覚を使うか」の分類論争に見合わないので、能力値【五感】での近似か、技能を直接選ぶ効果で代える
- [x] イニシアチブ管理のCombat統合（Issue #80）
- 技能集のコンペンディウム同梱・FoundryVTT公式への登録・README整備（Issue #84）。リリースワークフローは整備済み

### 正式版に向けて

- ガイアケア対応
    - 差分を調べる
- [x] ActiveEffectを誰にでも使いやすく（属性キーを「対象 × 修正先」の選択式に、全体修正 `system.mod` の追加、ルールブックの状態異常の登録、`phase` の選択）
    - 共鳴判定への修正の着地点（`resources.resonance.mod`）まで用意してある。残響の器そのものはIssue化を見送った
- [x] 自動化の程度を設定で可変にする。防具の自動軽減・HP/MP境界の案内・生存リマインダ・共鳴値の自動上昇・感情マッチングの自動判定・判定ダイアログの既定を個別に切れる（既定はすべて現状どおり）
- ある程度の自動化（Issue #85 の範囲まで）

## 並行プロジェクト

- FoundryVTT開発ドキュメントの日本語整備（別リポジトリ: `fvtt-dev-docs-ja`）

## 実装しないこと

- 高度な自動化
