{{! Weapon Card }}
<div class="em-weapon-card">
  <header class="em-weapon-card__head">
    <img class="em-avatar em-weapon-card__image" src="{{weaponImg}}" height="36" width="36" alt="" />
    <div class="em-weapon-card__title">
      <div class="em-weapon-card__name">{{weaponName}}</div>
      <div class="em-meta em-weapon-card__meta">
        {{rangeLabel}}
        <span class="em-weapon-card__sep">/</span>
        {{skillLabel}}
        {{#if attackPower}}
          <span class="em-weapon-card__sep">/</span>
          {{attackPower}}
        {{/if}}
      </div>
    </div>
  </header>

  {{! ロールは本体の描画をそのまま埋める。content に要素があるので二重には描かれない }}
  {{#if attackHTML}}
    <section class="em-weapon-card__roll">
      <h4 class="em-meta em-weapon-card__roll-label">{{localize "EMOKLORE.ChatMessage.weapon.Attack"}}</h4>
      {{{attackHTML}}}
    </section>
  {{/if}}

  {{#if damageHTML}}
    <section class="em-weapon-card__roll">
      <h4 class="em-meta em-weapon-card__roll-label">{{localize "EMOKLORE.ChatMessage.weapon.Damage"}}</h4>
      {{{damageHTML}}}
    </section>
  {{/if}}

  {{#if (or canRollAttack canRollDamage canApplyDamage)}}
    <footer class="em-weapon-card__buttons">
      {{#if canRollAttack}}
        <button type="button" data-action="rollAttack">
          <i class="fa-solid fa-hand-fist" inert></i>
          <span>{{localize "EMOKLORE.ChatMessage.weapon.Attack"}}</span>
        </button>
      {{/if}}
      {{#if canRollDamage}}
        <button type="button" data-action="rollDamage">
          <i class="fa-solid fa-burst" inert></i>
          <span>{{localize "EMOKLORE.ChatMessage.weapon.Damage"}}</span>
        </button>
      {{/if}}
      {{#if canApplyDamage}}
        {{! 対象は押した瞬間の盤面から取るので、何度でも押せる }}
        <button type="button" data-action="applyDamage">
          <i class="fa-solid fa-heart-crack" inert></i>
          <span>{{localize "EMOKLORE.ChatMessage.weapon.Apply"}}</span>
        </button>
        <button type="button" data-action="applyDamageWithReduction">
          <i class="fa-solid fa-shield-halved" inert></i>
          <span>{{localize "EMOKLORE.ChatMessage.weapon.ApplyWithReduction"}}</span>
        </button>
      {{/if}}
    </footer>
  {{/if}}
</div>
