
{{#if isPlay}}
<div class="baseskills">
  {{#unless isPlay}}
  <h2>{{localize "EMOKLORE.baseSkill"}}</h2>
  {{/unless}}
  {{#each system.baseSkills as |baseSkill key|}}
  <div class="baseskill" data-baseskill="{{key}}">
    <a class="label" 
      data-action="roll" 
      data-roll-type="skill" 
      data-skill="{{key}}">
      {{baseSkill.label}} | {{baseSkill.target}}
    </a>
  </div>
  {{/each}}
</div>
{{/if}}
